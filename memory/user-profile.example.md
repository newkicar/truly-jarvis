# 用户资料（示例模板）

复制本文件为 `user-profile.md` 并填入你自己的资料；jarvis 启动时会自动注入 `memory/*.md` 到上下文。

示例格式（按需增删，写自然语言即可）：

- 职业：产品经理，技术背景
- 关注点：AI 大模型技术趋势、某个具体产品方向
- 偏好：输出用中文、结论先行、重要数据标注来源
- 正在跟进的事项：某个项目、某门课程（可附 notes 路径）

> 提示：信息记忆（偏好/职业/行业）放 `memory/`；知识类内容放 Obsidian vault 的 `/vault/`。